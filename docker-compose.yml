services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: malaysia-lottery-checker

    ports:
      - "8080:8080"

    volumes:
      - .:/app

    working_dir: /app

    command: go run .